services:
  - type: web
    name: nairi-ventures
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node serve.js
    envVars:
      - key: NODE_ENV
        value: production
